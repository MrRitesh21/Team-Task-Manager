{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Team Task Manager Monorepo",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build:all": "npm run build --prefix client"
  }
}
